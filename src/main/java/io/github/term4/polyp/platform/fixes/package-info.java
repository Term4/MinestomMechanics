/**
 * Behavior CORRECTIONS: server- or client-side bugs and vanilla divergences patched to the intended behavior
 * ({@code fixes.client} = per-client-version quirks). Faithful cross-version translation of correct behavior
 * belongs in {@code platform.compatibility} instead.
 */
package io.github.term4.polyp.platform.fixes;
