/**
 * Cross-version TRANSLATION: making what the server already does reach every client faithfully (Via bridges,
 * protocol encodings, Animatium, offhand/reach shims). A workaround for a client or server BUG belongs in
 * {@code platform.fixes} instead.
 */
package io.github.term4.polyp.platform.compatibility;
